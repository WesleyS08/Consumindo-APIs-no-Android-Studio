<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E3DADA"
    android:padding="16dp">

    <EditText
        android:id="@+id/editText_search"
        style="@style/EditTextHint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        android:layout_marginBottom="16dp"
        android:autoText="false"
        android:backgroundTint="#D60A0A"
        android:foregroundTint="#8C5454"
        android:hint="Digite o título do filme"
        android:imeOptions="actionSearch"
        android:inputType="text"
        android:selectAllOnFocus="false"
        android:singleLine="false"
        android:textAlignment="textStart"
        android:textColorHint="#000000"
        android:textColorLink="#600606"
        android:textStyle="normal" />

    <Button
        android:id="@+id/button_search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/editText_search"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="150dp"
        android:textColor="@color/white"
        android:backgroundTint="#5318BC"
        android:text="Pesquisar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/button_search"
        android:layout_marginTop="16dp">

        <LinearLayout
            android:id="@+id/linear_layout_results"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Resultados da pesquisa serão adicionados dinamicamente aqui -->

        </LinearLayout>

    </ScrollView>

</RelativeLayout>
